import React from "react";

export default (props: any) => {
     return (
          <div className="p-4 w-full flex justify-center bg-[#181818] text-zinc-200">
               {props.text}
          </div>
     )

}